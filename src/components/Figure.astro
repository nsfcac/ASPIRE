---
interface Props {
  id?: string;
  title: string;
  caption?: string;
}
const { id, title, caption } = Astro.props;
---

<figure class="fig" id={id}>
  <div class="fig-body">
    <slot />
  </div>
  <figcaption>
    <b>{title}</b>{caption ? ` ${caption}` : ''}
    <slot name="caption" />
  </figcaption>
</figure>
